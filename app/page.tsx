'use client';
import {useEffect,useMemo,useState} from 'react';
type Job={id:string;title:string;link:string;score:number;salary:string|null;date:string;status:string};
type AppState={id:string;title:string;status:string;date:string};
type Saved={title:string;url:string;status:string;score:number;date:string};
const norm=(s:string)=>s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');
function answer(q:string){const t=norm(q);if(/salario|pretension|expectativa/.test(t))return 'Mi expectativa salarial está en torno a 20.000–24.000 € brutos anuales, aunque puedo valorarlo según funciones, horario, modalidad y posibilidades de crecimiento.';if(/experiencia/.test(t))return 'Tengo experiencia en soporte IT, resolución de incidencias, mantenimiento de equipos y plataformas web, redes, copias de seguridad, bases de datos y tareas técnicas de configuración y diagnóstico.';if(/por que|por qué|motivacion|interesa/.test(t))return 'Me interesa porque encaja con mi perfil técnico de soporte IT y desarrollo web y me permitiría seguir creciendo en sistemas, infraestructura y resolución de incidencias.';if(/ingles|english/.test(t))return 'Tengo un nivel de inglés intermedio y puedo desenvolverme con documentación técnica y comunicación básica en entorno profesional.';if(/universit|grado/.test(t))return 'No tengo grado universitario. Tengo Grado Superior en Desarrollo de Aplicaciones Web y Grado Medio en Sistemas Microinformáticos y Redes, además de formación en ciberseguridad.';if(/disponibilidad|incorpor/.test(t))return 'Tengo disponibilidad para incorporarme según las necesidades de la empresa.';if(/carnet|vehiculo|coche/.test(t))return 'Sí, tengo carnet de conducir y vehículo propio.';return 'Tengo formación en DAW y SMR y experiencia práctica en soporte IT, incidencias, hardware/software, redes, plataformas web y mantenimiento técnico. Puedo ampliar cualquier punto que necesitéis.'}
export default function Home(){const[jobs,setJobs]=useState<Job[]>([]),[apps,setApps]=useState<AppState[]>([]),[connected,setConnected]=useState<boolean|null>(null),[busy,setBusy]=useState(true),[saved,setSaved]=useState<Saved[]>([]),[filter,setFilter]=useState('Todas'),[q,setQ]=useState(''),[copied,setCopied]=useState(false);useEffect(()=>{try{setSaved(JSON.parse(localStorage.getItem('autojobs-cards')||'[]'))}catch{};load()},[]);useEffect(()=>localStorage.setItem('autojobs-cards',JSON.stringify(saved)),[saved]);async function load(){setBusy(true);try{const r=await fetch('/api/jobs',{cache:'no-store'}),d=await r.json();setConnected(!!d.connected);setJobs(d.jobs||[]);setApps(d.applications||[]);setSaved(old=>old.map(x=>{const a=(d.applications||[]).find((y:AppState)=>norm(y.title).includes(norm(x.title))||norm(x.title).includes(norm(y.title)));return a?{...x,status:a.status}:x}))}catch{setConnected(false)}finally{setBusy(false)}}const local=(j:Job)=>saved.find(x=>norm(x.title)===norm(j.title));const state=(j:Job)=>local(j)?.status||j.status;function apply(j:Job){setSaved(v=>{const exists=v.some(x=>norm(x.title)===norm(j.title));return exists?v.map(x=>norm(x.title)===norm(j.title)?{...x,status:'Abierta',url:j.link||x.url}:x):[{title:j.title,url:j.link,status:'Abierta',score:j.score,date:new Date().toLocaleDateString('es-ES')},...v]});window.open(j.link,'_blank')}function dismiss(j:Job){setSaved(v=>[{title:j.title,url:j.link,status:'Descartado',score:j.score,date:new Date().toLocaleDateString('es-ES')},...v.filter(x=>norm(x.title)!==norm(j.title))])}const visible=useMemo(()=>jobs.filter(j=>{const s=saved.find(x=>norm(x.title)===norm(j.title))?.status||j.status;if(s==='Descartado')return false;return filter==='Todas'||s===filter}),[jobs,filter,saved]);const quick=answer(q);const activeCount=jobs.filter(j=>{const s=state(j);return s!=='Descartado'&&s!=='Finalizado'}).length;
return <main><header className="appHeader"><div><div className="brand">AUTOJOBS</div><div className="topline"><h1>Ofertas</h1><button className="iconBtn" onClick={load} aria-label="Actualizar">↻</button></div><p className="sub">{busy?'Actualizando ofertas…':`${activeCount} oportunidades relacionadas con tu perfil`}</p></div></header>{busy?<div className="skeletonList"><div/><div/><div/></div>:connected?<><div className="filters">{['Todas','Nueva','Abierta','Inscrito','Novedades'].map(f=><button key={f} className={filter===f?'active':''} onClick={()=>setFilter(f)}>{f}</button>)}</div><section className="list">{visible.length===0?<div className="empty"><b>No hay ofertas aquí</b><span>Prueba otro filtro o actualiza el feed.</span></div>:visible.map(j=>{const s=state(j);return <article className="job" key={j.id}><div className="jobMain"><div className={`scorePill ${j.score>=80?'scoreGood':j.score>=65?'scoreMid':''}`}>{j.score}%</div><div className="jobInfo"><h3>{j.title}</h3><div className="jobMeta"><span className={j.salary?'salary salaryKnown':'salary'}>💰 {j.salary||'No indicado'}</span><span className={`status ${s.toLowerCase()}`}>{s}</span></div></div></div><div className="jobActions"><button className="ghost" onClick={()=>dismiss(j)}>Descartar</button><button onClick={()=>apply(j)}>{s==='Abierta'?'Continuar':'Aplicar'}</button></div></article>})}</section></>:<section className="card compact"><b>Gmail no está conectado</b><p className="small">Conéctalo para que AutoJobs lea tus alertas y estados de InfoJobs.</p><a className="btn" href="/api/auth/google/start">Conectar Gmail</a></section>}<details className="card compact"><summary>Mis candidaturas <span className="count">{saved.filter(x=>x.status!=='Descartado').length}</span></summary><div className="detailsBody">{saved.filter(x=>x.status!=='Descartado').length===0?<p className="small">Cuando pulses Aplicar aparecerán aquí.</p>:saved.filter(x=>x.status!=='Descartado').slice(0,25).map((x,i)=><div className="saved" key={i}><div><b>{x.title}</b><span className={`status ${x.status.toLowerCase()}`}>{x.status}</span></div><a href={x.url} target="_blank">Abrir</a></div>)}</div></details><details className="card compact"><summary>Ayuda con preguntas de candidatura</summary><div className="detailsBody"><p className="small">Pega la pregunta que te haga InfoJobs y preparo una respuesta basada en tu CV.</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Ej. ¿Cuál es tu experiencia en soporte IT?"/>{q&&<><div className="answer">{quick}</div><button className="secondary" onClick={async()=>{await navigator.clipboard.writeText(quick);setCopied(true);setTimeout(()=>setCopied(false),1200)}}>{copied?'✓ Copiada':'Copiar respuesta'}</button></>}</div></details>{apps.length>0&&<details className="card compact mutedDetails"><summary>Actividad detectada en InfoJobs</summary><div className="detailsBody">{apps.slice(0,15).map(a=><div className="saved" key={a.id}><div><b>{a.title}</b><span className={`status ${a.status.toLowerCase()}`}>{a.status}</span></div></div>)}</div></details>}</main>}
