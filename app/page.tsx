'use client';
import {useEffect,useMemo,useState} from 'react';
type Job={id:string;title:string;link:string;score:number;salary:string|null;date:string;status:string};
type AppState={id:string;title:string;status:string;date:string};
type Saved={title:string;url:string;status:string;score:number;date:string};
const norm=(s:string)=>s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');
function answer(q:string){const t=norm(q);if(/salario|pretension|expectativa/.test(t))return 'Mi expectativa salarial está en torno a 20.000–24.000 € brutos anuales, aunque puedo valorarlo según funciones, horario, modalidad y posibilidades de crecimiento.';if(/experiencia/.test(t))return 'Tengo experiencia en soporte IT, resolución de incidencias, mantenimiento de equipos y plataformas web, redes, copias de seguridad, bases de datos y tareas técnicas de configuración y diagnóstico.';if(/por que|por qué|motivacion|interesa/.test(t))return 'Me interesa porque encaja con mi perfil técnico de soporte IT y desarrollo web y me permitiría seguir creciendo en sistemas, infraestructura y resolución de incidencias.';if(/ingles|english/.test(t))return 'Tengo un nivel de inglés intermedio y puedo desenvolverme con documentación técnica y comunicación básica en entorno profesional.';if(/universit|grado/.test(t))return 'No tengo grado universitario. Tengo Grado Superior en Desarrollo de Aplicaciones Web y Grado Medio en Sistemas Microinformáticos y Redes, además de formación en ciberseguridad.';if(/disponibilidad|incorpor/.test(t))return 'Tengo disponibilidad para incorporarme según las necesidades de la empresa.';if(/carnet|vehiculo|coche/.test(t))return 'Sí, tengo carnet de conducir y vehículo propio.';return 'Tengo formación en DAW y SMR y experiencia práctica en soporte IT, incidencias, hardware/software, redes, plataformas web y mantenimiento técnico. Puedo ampliar cualquier punto que necesitéis.'}
export default function Home(){const[jobs,setJobs]=useState<Job[]>([]),[apps,setApps]=useState<AppState[]>([]),[connected,setConnected]=useState<boolean|null>(null),[busy,setBusy]=useState(true),[saved,setSaved]=useState<Saved[]>([]),[filter,setFilter]=useState('Todas'),[q,setQ]=useState(''),[copied,setCopied]=useState(false);useEffect(()=>{try{setSaved(JSON.parse(localStorage.getItem('autojobs-cards')||'[]'))}catch{};load()},[]);useEffect(()=>localStorage.setItem('autojobs-cards',JSON.stringify(saved)),[saved]);async function load(){setBusy(true);try{const r=await fetch('/api/jobs',{cache:'no-store'}),d=await r.json();setConnected(!!d.connected);setJobs(d.jobs||[]);setApps(d.applications||[]);setSaved(old=>old.map(x=>{const a=(d.applications||[]).find((y:AppState)=>norm(y.title).includes(norm(x.title))||norm(x.title).includes(norm(y.title)));return a?{...x,status:a.status}:x}))}catch{setConnected(false)}finally{setBusy(false)}}function apply(j:Job){setSaved(v=>{const exists=v.some(x=>norm(x.title)===norm(j.title));return exists?v.map(x=>norm(x.title)===norm(j.title)?{...x,status:'Abierta'}:x):[{title:j.title,url:j.link,status:'Abierta',score:j.score,date:new Date().toLocaleDateString('es-ES')},...v]});window.open(j.link,'_blank')}function dismiss(j:Job){setSaved(v=>[{title:j.title,url:j.link,status:'Descartado',score:j.score,date:new Date().toLocaleDateString('es-ES')},...v.filter(x=>norm(x.title)!==norm(j.title))])}const visible=useMemo(()=>jobs.filter(j=>filter==='Todas'||j.status===filter),[jobs,filter]);const quick=answer(q);
return <main><header><div className="brand">AUTOJOBS</div><div className="topline"><h1>Ofertas</h1><button className="iconBtn" onClick={load}>↻</button></div><p className="sub">Feed de InfoJobs desde Gmail, con estados y respuestas rápidas.</p></header>{busy?<div className="card compact">⏳ Actualizando…</div>:connected?<><div className="filters">{['Todas','Nueva','Inscrito','Novedades','Finalizado'].map(f=><button key={f} className={filter===f?'active':''} onClick={()=>setFilter(f)}>{f}</button>)}</div><section className="list">{visible.length===0?<div className="card compact">No hay ofertas en este filtro.</div>:visible.map(j=><article className="job" key={j.id}><div className="jobMain"><div className="scorePill">{j.score}%</div><div><h3>{j.title}</h3><div className="jobMeta"><span className="salary">💰 {j.salary||'Salario no indicado'}</span><span className={`status ${j.status.toLowerCase()}`}>{j.status}</span></div></div></div><div className="jobActions"><button className="ghost" onClick={()=>dismiss(j)}>Descartar</button><button onClick={()=>apply(j)}>Aplicar</button></div></article>)}</section></>:<section className="card compact"><p>Conecta Gmail para leer las alertas y estados de InfoJobs.</p><a className="btn" href="/api/auth/google/start">Conectar Gmail</a></section>}<section className="card compact"><div className="topline"><h2>Mis candidaturas</h2><span className="count">{saved.length}</span></div>{saved.length===0?<p className="small">Cuando pulses Aplicar aparecerán aquí.</p>:saved.slice(0,20).map((x,i)=><div className="saved" key={i}><div><b>{x.title}</b><span className={`status ${x.status.toLowerCase()}`}>{x.status}</span></div><a href={x.url} target="_blank">Abrir</a></div>)}</section><section className="card compact"><h2>Responder preguntas</h2><p className="small">Si InfoJobs te pregunta algo al aplicar, escribe aquí la pregunta y AutoJobs prepara una respuesta usando tu CV.</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Ej. ¿Cuál es tu experiencia en soporte IT?"/>{q&&<><div className="answer">{quick}</div><button className="secondary" onClick={async()=>{await navigator.clipboard.writeText(quick);setCopied(true);setTimeout(()=>setCopied(false),1200)}}>{copied?'✓ Copiada':'Copiar respuesta'}</button></>}</section>{apps.length>0&&<details className="card compact"><summary>Estados detectados por correo</summary>{apps.slice(0,15).map(a=><div className="saved" key={a.id}><div><b>{a.title}</b><span className={`status ${a.status.toLowerCase()}`}>{a.status}</span></div></div>)}</details>}</main>}
