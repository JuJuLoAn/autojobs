'use client';
import {useEffect,useMemo,useState} from 'react';
type Job={id:string;title:string;link:string;score:number;salary:string|null;date:string;status:string};
type AppState={id:string;title:string;status:string;date:string};
type Saved={title:string;url:string;status:string;score:number;date:string};
type QA={question:string;answer:string;busy?:boolean};
const norm=(s:string)=>s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');
export default function Home(){const[jobs,setJobs]=useState<Job[]>([]),[apps,setApps]=useState<AppState[]>([]),[connected,setConnected]=useState<boolean|null>(null),[busy,setBusy]=useState(true),[saved,setSaved]=useState<Saved[]>([]),[filter,setFilter]=useState('Todas'),[qa,setQa]=useState<Record<string,QA>>({}),[copied,setCopied]=useState('');useEffect(()=>{try{setSaved(JSON.parse(localStorage.getItem('autojobs-cards')||'[]'));setQa(JSON.parse(localStorage.getItem('autojobs-qa')||'{}'))}catch{};load();const onShow=()=>{if(document.visibilityState==='visible')load()};document.addEventListener('visibilitychange',onShow);window.addEventListener('pageshow',onShow);return()=>{document.removeEventListener('visibilitychange',onShow);window.removeEventListener('pageshow',onShow)}},[]);useEffect(()=>localStorage.setItem('autojobs-cards',JSON.stringify(saved)),[saved]);useEffect(()=>localStorage.setItem('autojobs-qa',JSON.stringify(qa)),[qa]);async function load(){setBusy(true);try{const r=await fetch('/api/jobs',{cache:'no-store'}),d=await r.json();setConnected(!!d.connected);setJobs(d.jobs||[]);setApps(d.applications||[]);setSaved(old=>old.map(x=>{const a=(d.applications||[]).find((y:AppState)=>norm(y.title).includes(norm(x.title))||norm(x.title).includes(norm(y.title)));return a?{...x,status:a.status}:x}))}catch{setConnected(false)}finally{setBusy(false)}}const local=(j:Job)=>saved.find(x=>norm(x.title)===norm(j.title));const state=(j:Job)=>local(j)?.status||j.status;function apply(j:Job){setSaved(v=>{const exists=v.some(x=>norm(x.title)===norm(j.title));return exists?v.map(x=>norm(x.title)===norm(j.title)?{...x,status:'Abierta',url:j.link||x.url}:x):[{title:j.title,url:j.link,status:'Abierta',score:j.score,date:new Date().toLocaleDateString('es-ES')},...v]});sessionStorage.setItem('autojobs-return-title',j.title);window.location.assign(j.link)}function dismiss(j:Job){setSaved(v=>[{title:j.title,url:j.link,status:'Descartado',score:j.score,date:new Date().toLocaleDateString('es-ES')},...v.filter(x=>norm(x.title)!==norm(j.title))])}async function generate(j:Job){const k=norm(j.title),current=qa[k]||{question:'',answer:''};if(!current.question.trim())return;setQa(v=>({...v,[k]:{...current,busy:true,answer:''}}));try{const r=await fetch('/api/answer',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:j.title,question:current.question})});const d=await r.json();setQa(v=>({...v,[k]:{question:current.question,answer:d.answer||d.error||'No pude preparar la respuesta.',busy:false}}))}catch{setQa(v=>({...v,[k]:{question:current.question,answer:'No pude preparar la respuesta.',busy:false}}))}}const visible=useMemo(()=>jobs.filter(j=>{const s=saved.find(x=>norm(x.title)===norm(j.title))?.status||j.status;if(s==='Descartado')return false;return filter==='Todas'||s===filter}),[jobs,filter,saved]);const activeCount=jobs.filter(j=>{const s=state(j);return s!=='Descartado'&&s!=='Finalizado'}).length;
return <main><header className="appHeader"><div><div className="brand">AUTOJOBS</div><div className="topline"><h1>Ofertas</h1><button className="iconBtn" onClick={load} aria-label="Actualizar">↻</button></div><p className="sub">{busy?'Actualizando ofertas…':`${activeCount} oportunidades relacionadas con tu perfil`}</p></div></header>{busy?<div className="skeletonList"><div/><div/><div/></div>:connected?<><div className="filters">{['Todas','Nueva','Abierta','Inscrito','Novedades'].map(f=><button key={f} className={filter===f?'active':''} onClick={()=>setFilter(f)}>{f}</button>)}</div><section className="list">{visible.length===0?<div className="empty"><b>No hay ofertas aquí</b><span>Prueba otro filtro o actualiza el feed.</span></div>:visible.map(j=>{const s=state(j),k=norm(j.title),item=qa[k]||{question:'',answer:''};return <article className="job" key={j.id}><div className="jobMain"><div className={`scorePill ${j.score>=80?'scoreGood':j.score>=65?'scoreMid':''}`}>{j.score}%</div><div className="jobInfo"><h3>{j.title}</h3><div className="jobMeta"><span className={j.salary?'salary salaryKnown':'salary'}>💰 {j.salary||'No indicado'}</span><span className={`status ${s.toLowerCase()}`}>{s}</span></div></div></div><div className="jobActions"><button className="ghost" onClick={()=>dismiss(j)}>Descartar</button><button onClick={()=>apply(j)}>{s==='Abierta'?'Continuar':'Aplicar'}</button></div><details className="jobQa" open={s==='Abierta'}><summary>Preguntas de esta oferta</summary><div className="jobQaBody"><textarea value={item.question} onChange={e=>setQa(v=>({...v,[k]:{...item,question:e.target.value}}))} placeholder="Pega aquí la pregunta exacta que te haga InfoJobs"/><button className="secondary" onClick={()=>generate(j)} disabled={!item.question.trim()||item.busy}>{item.busy?'Preparando…':'Responder con mi CV'}</button>{item.answer&&<><div className="answer">{item.answer}</div><button className="ghost" onClick={async()=>{await navigator.clipboard.writeText(item.answer);setCopied(k);setTimeout(()=>setCopied(''),1200)}}>{copied===k?'✓ Copiada':'Copiar respuesta'}</button></>}</div></details></article>})}</section></>:<section className="card compact"><b>Gmail no está conectado</b><p className="small">Conéctalo para que AutoJobs lea tus alertas y estados de InfoJobs.</p><a className="btn" href="/api/auth/google/start">Conectar Gmail</a></section>}<details className="card compact"><summary>Mis candidaturas <span className="count">{saved.filter(x=>x.status!=='Descartado').length}</span></summary><div className="detailsBody">{saved.filter(x=>x.status!=='Descartado').length===0?<p className="small">Cuando pulses Aplicar aparecerán aquí.</p>:saved.filter(x=>x.status!=='Descartado').slice(0,25).map((x,i)=><div className="saved" key={i}><div><b>{x.title}</b><span className={`status ${x.status.toLowerCase()}`}>{x.status}</span></div><a href={x.url}>Abrir</a></div>)}</div></details>{apps.length>0&&<details className="card compact mutedDetails"><summary>Actividad detectada en InfoJobs</summary><div className="detailsBody">{apps.slice(0,15).map(a=><div className="saved" key={a.id}><div><b>{a.title}</b><span className={`status ${a.status.toLowerCase()}`}>{a.status}</span></div></div>)}</div></details>}</main>}
